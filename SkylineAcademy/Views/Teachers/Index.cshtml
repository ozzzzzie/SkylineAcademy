@model IEnumerable<SkylineAcademy.Models.Teacher>

@{
    ViewData["Title"] = "Teachers";
}

<br />

<p>
   <a asp-area="" asp-controller="AdministratorDashboard" asp-action="Index"><button type="button" class="btn btn-outline-primary">Back to Dashboard</button></a>
    <a asp-area="" asp-controller="TeacherDashboard" asp-action="Index"><button type="button" class="btn btn-outline-primary">Back to Dashboard</button></a>
    <a asp-area="" asp-controller="StudentDashboard" asp-action="Index"><button type="button" class="btn btn-outline-primary">Back to Dashboard</button></a>
</p>
<h1 align="center">Teachers</h1>
<br />
<p align="right">
    <a asp-action="Create"><button type="button" class="btn btn-outline-primary">Create New</button></a>
</p>

<style>
    .grid-container {
        overflow: hidden;
    }
    .grid-container table {
        table-layout: fixed;
        width: 100%;
    }
    .grid-container table td {
        white-space: normal;
        word-wrap: break-word;
    }
</style>

<div class="table grid-container">
    @(
    Html.Grid(Model)
        .Build(columns =>
        {
            columns.Add(model => model.Tfname).Titled("First Name");
            columns.Add(model => model.Tlname).Titled("Last Name");
            columns.Add(model => model.Taddress).Titled("Address");
            columns.Add(model => model.Tphonenumber).Titled("Phone Number");
            columns.Add(model => model.Email).Titled("Email");
            columns.Add(model => model.Ishead).Titled("Head");
            columns.Add(model => model.Faculty.Fname).Titled("Faculty");
            columns.Add().RenderedAs(model => Html.ActionLink("Edit", "Edit", new { id = model.TeacherId })).Filterable(false);
            columns.Add().RenderedAs(model => Html.ActionLink("Details", "Details", new { id = model.TeacherId })).Filterable(false);
            columns.Add().RenderedAs(model => Html.ActionLink("Delete", "Delete", new { id = model.TeacherId })).Filterable(false);
        })
        .Empty("No data found")
        .Filterable()
        .Sortable()
        .Pageable(pager =>
        {
            pager.RowsPerPage = 10;
        })
    )
</div>

@section Scripts {
    <script>
        document.querySelectorAll(".mvc-grid").forEach(element => new MvcGrid(element));
    </script>
}